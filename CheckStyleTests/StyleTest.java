import static java.lang.Math.*;

public class StyleTest{
	
	public void longMethod() {
		
		int x = 1;
		int a = 1;
		int v = 1;
		int z = 1;
		int babf = 1;
		int b = 1;
		int ava = 1;
		int y = 1;
		int o = 1;
		int p = 1;
		int m = 1;
		int s = 1;
		int d = 1;
		int f = 1;
		int g = 1;
		int h = 1;
		int baba = 1;
		int bsbs = 1;
		int snsn = 1;
		int snc = 1;
		
	}
	
	public void returnCheck() {
		if(true) {
			int x = 1;
			return;
		}
		if(true) {
			int a = 2;
			return;
		}
		if(true) {
			int b = 3;
			return;
		}
		if(true) {
			int c = 4;
			return;
		}
	}
	/*
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 * Comments for ratio violation
	 */
		
}

		
	
	
